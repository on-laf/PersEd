<h1>Welcome as a new student</h1>

      <%= simple_form_for @student do |f| %>
        <%= f.input :first_name %>
        <%= f.input :last_name %>
        <%= f.button :submit, class: 'btn btn-primary' %>
      <% end %>
